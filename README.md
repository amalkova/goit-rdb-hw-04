# goit-rdb-hw-04  
Домашнє завдання до Теми 4  
**DML та DDL команди. Складні SQL-запити**

---

## 📌 Опис роботи

У цьому завданні була створена та заповнена тестовими даними база даних **LibraryManagement**, а також виконані складні SQL-запити з використанням `JOIN`, `GROUP BY`, `HAVING`, `LIMIT` та `OFFSET`.

---

## 1️⃣ Створення структури БД (DDL)

Створено схему **LibraryManagement** та таблиці:

- `authors`
- `genres`
- `books`
- `users`
- `borrowed_books`

Для таблиць визначені:
- первинні ключі (`PRIMARY KEY`)
- зовнішні ключі (`FOREIGN KEY`)
- зв’язки між таблицями

---

## 2️⃣ Заповнення даними (DML)

Усі таблиці заповнені простими тестовими даними  
(1–2 рядки в кожній таблиці) для перевірки коректності звʼязків і запитів.

---

## 3️⃣ Об’єднання таблиць (JOIN)

Виконано запит з використанням `INNER JOIN`, який об’єднує всі таблиці:

- `order_details`
- `orders`
- `customers`
- `products`
- `categories`
- `employees`
- `shippers`
- `suppliers`

Запит перевірено — результат успішно повертається.

---

## 🔄 INNER JOIN vs LEFT JOIN

Заміна окремих `INNER JOIN` на `LEFT JOIN` призводить до того, що:

> **Після заміни INNER JOIN на LEFT JOIN кількість рядків збільшується або залишається такою ж.  
LEFT JOIN повертає всі рядки з лівої таблиці `order_details`, навіть якщо у пов’язаних таблицях немає відповідного запису.  
INNER JOIN повертає тільки повністю співпадаючі рядки.**

---

## 4️⃣ Агрегація та фільтрація

На основі запиту з пункту 3 виконано:

- фільтрацію рядків, де `employee_id > 3 AND employee_id <= 10`
- групування за назвою категорії (`category_name`)
- підрахунок кількості рядків у групі
- обчислення середньої кількості товарів (`AVG(order_details.quantity)`)
- фільтрацію за умовою `HAVING AVG(quantity) > 21`
- сортування за спаданням кількості рядків
- використання `LIMIT` та `OFFSET` для виведення результатів

---

## ✅ Результат

Усі запити виконані коректно, результати підтверджені скріншотами, код структурований і готовий до перевірки.
